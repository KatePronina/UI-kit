mixin sliderRange(name, step, value)
    .slider-range(data-value= value, data-step=step, class=`slider--${name}`) 
        if name === 'flowing'
             span.slider-range__value(data-value= value)