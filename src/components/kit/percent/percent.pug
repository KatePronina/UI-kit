mixin percent({ value })
  -const currentPercent = (282.6 * +value) / 100
  svg.percent(aria-label= value + '%', viewBox="0 0 95 95")
    circle.percent__circle(r="45", cx="50%", cy="50%", stroke-dasharray=`${currentPercent} 282.6`)
    text.percent__text(x="50%", y="50%")= value