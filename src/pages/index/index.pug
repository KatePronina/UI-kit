include ../base.pug
include ../../components/button/button.pug
include ../../components/arrow-button/arrow-button.pug

+base('Index')
body
    .container-btn
        +button('button', 'green', 'standart', true)
        +button('button', 'orange', 'standart', true)
        +button('button', 'orange', 'big')
        +button('button', 'green', 'big')
    .container-btn
        +arrowBtn(true, 'next')
        +arrowBtn(true, 'prev')
        +arrowBtn(false, 'prev')
        +arrowBtn(false, 'next')